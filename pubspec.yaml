name: crypto_trader
description: Utilities for crpyto trading

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.3
  crypto: ^3.0.1
  provider: ^6.0.0
  intl: ^0.17.0
  path_provider: ^2.0.3
  fl_chart: ^0.40.0
  synchronized: ^3.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - config/config.json
